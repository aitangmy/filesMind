FilesMindApp target status

Phase 1 completed in Swift Package executable target:
- Sources/FilesMindApp contains runnable SwiftUI shell.
- Composition root uses AppCore.AppBootstrap.
- Entry scene currently includes:
  1) Workspace picker + bookmark authorization
  2) Import queue panel
  3) Mind map canvas host (QuadTree viewport filtering)

Next phase:
- Generate dedicated Xcode .xcodeproj app target.
- Wire real document pipeline + model manager onboarding UI.
- Integrate DesignSystem components and accessibility audit pass.
